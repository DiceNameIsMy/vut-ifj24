#!/bin/sh

# Set default CMake build options if not provided
CMAKE_FLAGS=""

if [ "$1" = "--prefix" ]; then
  CMAKE_FLAGS="-DCMAKE_INSTALL_PREFIX=$2"
fi

# Create a build directory
mkdir -p build

# Run CMake to configure the project
cd build || exit
cmake $CMAKE_FLAGS ..
